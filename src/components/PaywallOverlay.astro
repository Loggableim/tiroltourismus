---
// PaywallOverlay.astro — deaktiviert: Inhalte bleiben für alle Nutzer sichtbar.
// Premium-Angebote werden nur noch als Business-Option außerhalb der Detailseiten beworben.
export interface Props {
  name: string;
  slug: string;
}
---

<div class="paywall-overlay-removed" aria-hidden="true"></div>

<style is:global>
  .paywall-overlay-removed {
    display: none !important;
  }
</style>
