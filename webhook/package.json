{
  "name": "tirol-lemon-webhook",
  "version": "1.0.0",
  "description": "LemonSqueezy webhook handler for Tirol Tourismus — syncs subscriptions to JSON data files",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "crypto": "^1.0.1"
  }
}
