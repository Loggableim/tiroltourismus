[Unit]
Description=Tirol Tourismus Webhook and Newsletter Server
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/var/www/webhook
ExecStart=/usr/bin/node server.js
Restart=always
RestartSec=5
EnvironmentFile=/var/www/webhook/.env

[Install]
WantedBy=multi-user.target
